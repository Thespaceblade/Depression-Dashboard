# Core dependencies (required)
requests>=2.31.0
python-dateutil>=2.8.2
pytz>=2023.3

# Backend API (required)
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# Optional sports APIs (excluded from Vercel due to size - 250MB limit)
# These are imported conditionally in code and will gracefully degrade if missing
# Install locally if you need them:
#   pip install sportsipy>=0.4.0 nba_api>=1.2.1 fastf1>=3.1.0 espn-api>=0.23.0
# sportsipy>=0.4.0
# nba_api>=1.2.1
# fastf1>=3.1.0
# espn-api>=0.23.0
