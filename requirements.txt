# Core dependencies (required for serverless functions)
# Optimized for Vercel deployment - minimal dependencies to stay under 250MB limit
requests>=2.31.0
python-dateutil>=2.8.2
pytz>=2023.3

# Backend API (required for Railway/Flask deployment)
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# Sports APIs (required for full functionality)
# Note: These may be large but are needed for Railway deployment
sportsipy>=0.4.0
nba_api>=1.2.1
# fastf1 removed - using OpenF1 REST API instead (simpler, no dependencies)
espn-api>=0.23.0
